version: 2
jobs:
  build:
    working_directory: ~/site-aiki3
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run: sudo apt-get update
      - run: sudo apt-get install ncftp
      - restore_cache:
          keys:
            - aiki3-node10-v1-{{ checksum "yarn.lock" }}
            - aiki3-node10-v1-
      - run: yarn install
      - save_cache:
          key: aiki3-node10-v1-{{ checksum "yarn.lock" }}
          paths:
            - ~/site-aiki3/node_modules
      - run: yarn lint:js
      - run: yarn lint:hbs
      - run: yarn test
      - deploy:
          command: |
            if [[ "${CIRCLE_BRANCH}" == "master" ]]; then
              ./node_modules/.bin/ember build --environment=production
              ncftpput -R -v -u $FTP_USER -p $FTP_PASSWORD $FTP_HOST . dist/*
            fi
